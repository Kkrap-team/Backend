

activity_feed 테이블 생성 및 Entity 설계

폴더 생성 시 activity_feed 저장 로직 추가

팔로우 기반 피드 조회 API 구현 (GET /feed)

follows JOIN activity_feed JOIN folders JOIN users

Redis로 피드 결과 캐싱

Redis 캐시 만료 및 재생성 로직

응답 DTO 설계 (폴더 단위 묶음)

